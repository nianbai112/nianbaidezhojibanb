-- Backfill region columns required by the expanded admin area-management UI.
-- Older production databases were created before these fields existed in the
-- Prisma schema, so guarded ALTER statements keep the migration recoverable.
ALTER TABLE "regions"
  ADD COLUMN IF NOT EXISTS "logo" TEXT,
  ADD COLUMN IF NOT EXISTS "address" TEXT,
  ADD COLUMN IF NOT EXISTS "latitude" DOUBLE PRECISION,
  ADD COLUMN IF NOT EXISTS "longitude" DOUBLE PRECISION,
  ADD COLUMN IF NOT EXISTS "radius" INTEGER NOT NULL DEFAULT 5000,
  ADD COLUMN IF NOT EXISTS "distanceLimit" INTEGER DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "studentOnly" BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS "regionType" TEXT DEFAULT 'other',
  ADD COLUMN IF NOT EXISTS "isHot" BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS "regionCoverMode" TEXT DEFAULT 'cover',
  ADD COLUMN IF NOT EXISTS "balance" DECIMAL(12,2) DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "minWithdraw" DECIMAL(10,2) DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "maxWithdraw" DECIMAL(10,2) DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "withdrawFee" DECIMAL(10,2) DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "withdrawRate" DECIMAL(5,4) DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "commissionRate" DECIMAL(5,4) DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "selfUnbanFee" DECIMAL(10,2) DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "totalCommission" DECIMAL(12,2) DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "showHotList" BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS "hotFeaturedDisplay" TEXT DEFAULT 'none',
  ADD COLUMN IF NOT EXISTS "isForceGuidance" BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS "privateMessageEnabled" BOOLEAN NOT NULL DEFAULT true,
  ADD COLUMN IF NOT EXISTS "contactsRequireStudentAuth" BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS "onlyStudentAuthUsers" BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS "groupChatEnabled" BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS "enableQrcodeFilter" BOOLEAN NOT NULL DEFAULT false,
  ADD COLUMN IF NOT EXISTS "homeNavLayout" INTEGER DEFAULT 1,
  ADD COLUMN IF NOT EXISTS "messagePageLayout" TEXT DEFAULT 'default',
  ADD COLUMN IF NOT EXISTS "profilePageLayout" TEXT DEFAULT 'default',
  ADD COLUMN IF NOT EXISTS "carouselImages" JSONB,
  ADD COLUMN IF NOT EXISTS "regionTabs" JSONB,
  ADD COLUMN IF NOT EXISTS "homeLeaderboard" JSONB,
  ADD COLUMN IF NOT EXISTS "messageIcons" JSONB,
  ADD COLUMN IF NOT EXISTS "messageNavigation" JSONB,
  ADD COLUMN IF NOT EXISTS "profileLayoutItems" JSONB,
  ADD COLUMN IF NOT EXISTS "homeNavLayoutConfig" JSONB;
