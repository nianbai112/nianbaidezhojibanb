{
  "name": "lingmeng-site",
  "private": true,
  "version": "1.0.46-selfhosted.1",
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader native --host 0.0.0.0 --port 5174",
    "test": "node --test tests/*.test.mjs",
    "build": "vite build --configLoader native",
    "preview": "vite preview --configLoader native --host 0.0.0.0 --port 4174"
  },
  "dependencies": {
    "@vitejs/plugin-vue": "^6.0.7",
    "gsap": "^3.15.0",
    "vite": "^6.4.3",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
