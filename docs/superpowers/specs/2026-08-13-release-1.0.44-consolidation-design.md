# 1.0.44 功能整合与干净发布设计

## 目标

以已经生成 1.0.43 的 `/Users/nianbaidediannao/Desktop/后端后台干净版本` 为唯一发布基线，把已经本地验证但未进入 1.0.43 的校园地图采集导入、买家订单生命周期和骑手收入结算后端能力合并，生成可复现、无源码依赖和无垃圾文件的 1.0.44 宝塔更新包。

## 已批准边界

- 只合并已确认缺失的三个后端/后台闭环，不引入新运行时依赖，不做无关重构。
- 校园地图沿用现有草稿、发布、版本和回滚模型；导入只进入草稿，不自动发布。
- 买家订单继续由 `/order` 返回履约、退款和确认收货契约，保留现有支付和退款安全检查。
- 骑手收入接口只允许正式全职骑手访问，并受远程 `features.income` 开关控制；结算记录和申诉必须按当前骑手隔离。
- 保留 1.0.43 干净版中 2026-08-13 的校园采集对象模型与迁移；禁止用较旧工作区的完整 Prisma schema 覆盖。
- 小程序与骑手客户端不进入宝塔更新包。本批只保证服务器契约；客户端分别发布。
- 不直接部署生产服务器；先产出并校验 1.0.44 包。

## 合并策略

在 `/tmp` 建立隔离发布目录并复制 1.0.43 干净版。校园地图和订单按各自已提交代码变更逐提交应用，避免带入分支上的其他历史；骑手收入以本地主分支共同基线、1.0.43 干净版和骑手修复工作区进行三方合并，只处理明确变更文件。

若补丁上下文冲突，先比较共同基线和当前干净版，手工保留双方有效行为。任何工作区整文件替换若会删除当前 Prisma 字段、迁移或接口，均不得采用。

## 数据库

三个 Prisma schema 只追加 `RiderSettlement` 申诉字段和索引。MySQL、PostgreSQL additive migration 与 Prisma migration 必须同时存在，并由发布迁移检查覆盖。校园采集现有 20260813 迁移保持原样。

## 验收

- 校园地图导入、订单生命周期、骑手收入/结算专项测试通过。
- Prisma schema 校验、同步检查、后端构建、后台类型检查与构建、站点构建通过。
- 1.0.44 ZIP 完整，manifest 版本与 sourceRoot 正确，SHA256 可复算。
- ZIP 中不含 `node_modules`、源码、缓存、日志、`.DS_Store`、`*.tsbuildinfo`、临时工作区或客户端安装包。
- 解包后的运行代码包含三个新增契约及全部 20260813 数据库迁移。
