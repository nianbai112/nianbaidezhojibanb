# Release 1.0.44 Consolidation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Consolidate the locally verified campus-map import, buyer order lifecycle, and rider income contracts into a clean 1.0.44 server release.

**Architecture:** Start from the exact 1.0.43 clean source in an isolated temporary directory. Apply only the committed campus/order patches, three-way merge the rider files against their common baseline, add additive database migrations, then build and package from the verified staging tree.

**Tech Stack:** NestJS, Prisma, Jest, Vue 3, Vite, Node.js 22, Bash, ZIP.

## Global Constraints

- Do not add runtime dependencies.
- Preserve all existing 20260813 campus collection fields and migrations.
- Do not modify or deploy the mini-program or rider client in this server package.
- Do not deploy production in this plan.
- Every behavior change must retain or add a focused regression test.
- Package version is exactly `1.0.44`.

---

### Task 1: Build an Isolated Release Baseline

**Files:**
- Read: `/Users/nianbaidediannao/Desktop/后端后台干净版本/**`
- Stage: `/tmp/lingmeng-release-1.0.44.*/source/**`

**Interfaces:**
- Consumes: exact 1.0.43 clean source.
- Produces: isolated source tree with no Git metadata or dependencies.

- [ ] Copy the clean source while excluding `.git`, `node_modules`, caches, and `*.tsbuildinfo`.
- [ ] Record baseline version and confirm the current campus collection schema/migrations exist.
- [ ] Run the focused tests before merging and record any baseline failures.

### Task 2: Merge Campus Collection Objects into Map Draft

**Files:**
- Modify: `backend/src/modules/campus-map/campus-map-collection-import.ts`
- Modify: `backend/src/modules/campus-map/campus-map-collection-import.spec.ts`
- Modify: `backend/src/modules/campus-map/campus-map-collection.contract.ts`
- Modify: `backend/src/modules/campus-map/campus-map.controller.ts`
- Modify: `backend/src/modules/campus-map/campus-map.service.ts`
- Modify: related campus-map tests
- Modify: `admin/src/api/admin.ts`
- Modify: `admin/src/views/region/components/RegionCampusMapPainter.vue`
- Modify: `admin/src/views/region/components/campus-map/CampusMapCollectionDrawer.vue`
- Create: `admin/src/views/region/components/campus-map/CampusMapCollectionOverlay.vue`
- Modify: `admin/src/views/region/components/campus-map/campusMapCollectionModel.mjs`

**Interfaces:**
- Consumes: approved collection object IDs and current draft revision.
- Produces: `POST /admin/campus-map/collections/:regionId/import-to-draft` and an admin import overlay.

- [ ] Apply commits `d4c95e1`, `700b1a0`, `1ca53ec`, `80c6fc5`, `618cd52`, and `f38760d` in order.
- [ ] Run the import conversion, contract, controller, service, and admin model tests.
- [ ] Confirm import is approval-gated, revision-protected, idempotent, and draft-only.

### Task 3: Merge Buyer Order Lifecycle and Refund Closure

**Files:**
- Modify: `backend/src/modules/errand/errand.service.ts`
- Modify: `backend/src/modules/admin/admin.service.ts`
- Modify: `backend/src/modules/payment/payment.service.ts`
- Modify: `backend/src/modules/shop/shop.service.ts`
- Modify: their focused Jest tests
- Modify: `admin/src/views/merchant/MerchantRefunds.vue`
- Modify: `admin/src/views/modules/RefundsPage.vue`

**Interfaces:**
- Consumes: errand order, payment, rider, refund, and receipt state.
- Produces: `order_lifecycle`, `fulfillment_label`, `refund_label`, and `receipt_action` from buyer `/order`.

- [ ] Apply commits `481934d`, `6f8b9a2`, `b52812c`, `611bce1`, `0d7dcf7`, and `999edee` in order.
- [ ] Run errand, refund, payment, and shop order lifecycle tests.
- [ ] Confirm refund labels do not erase fulfillment state and receipt remains blocked until delivered without refund/risk holds.

### Task 4: Merge Rider Income, Settlement, and Appeal APIs

**Files:**
- Modify: `backend/src/modules/rider-app/rider-app.controller.ts`
- Modify: `backend/src/modules/rider-app/rider-app.service.ts`
- Modify: `backend/src/modules/rider-app/rider-app.module.ts`
- Modify: rider-app tests
- Modify: `backend/src/modules/finance-admin/finance-admin.controller.ts`
- Modify: `backend/src/modules/finance-admin/finance-admin.service.ts`
- Modify: finance-admin tests
- Modify: `backend/prisma/schema.prisma`
- Modify: `backend/prisma/schema.postgresql.prisma`
- Modify: `backend/prisma/schema.mysql.prisma`
- Create: rider settlement appeal migrations for Prisma, MySQL additive, and PostgreSQL additive paths.

**Interfaces:**
- Produces: `/rider-app/income/overview`, `/income/transactions`, `/settlements`, settlement detail/appeal, and withdrawal endpoints.
- Preserves: rider ownership, official-rider eligibility, feature flag, duplicate appeal idempotency, and admin appeal transition audit.

- [ ] Three-way merge only the seven rider/finance files changed from common base `39f847d` to `e0c647d`.
- [ ] Add only RiderSettlement appeal fields to the current schemas; never replace current schemas with the older branch files.
- [ ] Add MySQL and PostgreSQL additive migrations with idempotent guards compatible with the release migrator.
- [ ] Run migration contract, rider-app, finance-admin, and Prisma schema tests.

### Task 5: Build and Generate 1.0.44

**Files:**
- Modify: `deploy/VERSION`
- Generate: `/Users/nianbaidediannao/Desktop/更新包/lingmeng-baota-1.0.44.zip`
- Replace after verification: `/Users/nianbaidediannao/Desktop/后端后台干净版本`

**Interfaces:**
- Consumes: verified staged source.
- Produces: clean source tree and directly uploadable 1.0.44 Baota update ZIP.

- [ ] Set version to `1.0.44` and run Prisma schema sync/generation.
- [ ] Run focused suites, full backend tests, backend build, admin typecheck/build, site build, and release migration tests.
- [ ] Generate the package with `generate-baota-package.cjs --build`.
- [ ] Verify ZIP integrity, manifest, runtime markers, migrations, exclusions, file count, and SHA256.
- [ ] Replace the old desktop clean source only after all verification gates pass; retain no dependency or build-cache directories.
