# 市场版总审计报告 - 2026-07-16 修复记录待合并

原文件 `市场版总审计报告.md` 当前为逻辑大小 1,852,115 字节、物理块为 0 的稀疏占位文件，读取结果为空；同名干净版和备份文件也是同一状态。本记录不得替代原报告，待原文件恢复后按问题编号合并。

## AUD-P1-056

- 严重级别：P1
- 所属模块：微信支付终态同步、库存/名额释放
- 问题现象：未过期的微信 `NOTPAY` 不应关闭本地支付单；终态写入失败或并发失效时也不能投递业务释放任务。
- 证据文件和行号：[payment.service.ts](/Users/nianbaidediannao/Desktop/后端后台本地测试版/backend/src/modules/payment/payment.service.ts:432)、[payment.service.spec.ts](/Users/nianbaidediannao/Desktop/后端后台本地测试版/backend/src/modules/payment/payment.service.spec.ts:57)
- 根因判断：本地终态同步与释放任务投递之间没有以条件更新结果为门槛，可能在状态未真正落库时释放预约资源。
- 修改文件：`backend/src/modules/payment/payment.service.ts`、`backend/src/modules/payment/payment.service.spec.ts`。
- 修复思路：仅对过期 `NOTPAY`、`CLOSED`、`REVOKED`、`PAYERROR` 计算终态；以 `updateMany(id + pending/paying)` 作为一次性状态转换，只有实际更新一条记录才创建幂等释放任务。
- 覆盖业务链路：微信回调/主动查询/过期支付对账 -> PaymentOrder 终态 -> PaymentReservationRelease -> 订单库存、名额、跑腿或活动释放。
- 验证方式：`npm test -- --runInBand src/modules/payment/payment.service.spec.ts`，覆盖未过期 `NOTPAY`、已过期 `NOTPAY`、并发失去状态转换时不释放；`npm run build` 通过。
- 当前状态：已修。尚未进行真实微信商户沙箱或生产回调验证，不能标记“已验证”。

## AUD-P1-057

- 严重级别：P1
- 所属模块：微信支付下单重试、支付单号幂等
- 问题现象：过期支付单被微信确认关闭后仍可能复用旧单号；复用有效支付单时，微信 `time_expire` 与本地 `expireTime` 可能不一致。
- 证据文件和行号：[payment.service.ts](/Users/nianbaidediannao/Desktop/后端后台本地测试版/backend/src/modules/payment/payment.service.ts:193)、[payment.service.ts](/Users/nianbaidediannao/Desktop/后端后台本地测试版/backend/src/modules/payment/payment.service.ts:253)、[payment.service.spec.ts](/Users/nianbaidediannao/Desktop/后端后台本地测试版/backend/src/modules/payment/payment.service.spec.ts:95)
- 根因判断：旧实现没有在微信确认终态后清空可复用对象，且每次请求重新生成微信过期时间。
- 修改文件：`backend/src/modules/payment/payment.service.ts`、`backend/src/modules/payment/payment.service.spec.ts`。
- 修复思路：过期单先查询微信；成功支付走成功处理，确认 `closed/failed` 后清空旧单并创建新 `paymentNo`，其余状态拒绝重试以防双单；有效单的微信过期时间直接使用本地 `expireTime`。
- 覆盖业务链路：小程序支付重试 -> 统一下单 -> 微信订单查询 -> 本地终态同步 -> 新/旧支付单号返回。
- 验证方式：定向测试覆盖有效单复用过期时间、过期单经微信 `NOTPAY` 确认后生成新单号；`npm run build` 通过。
- 当前状态：已修。尚未进行小程序真实 `wx.requestPayment` 与微信回调联调，不能标记“已验证”。

## NEW-P1-20260716-001

- 严重级别：P1
- 所属模块：后端构建与 Prisma 运行前置
- 问题现象：直接执行 `npm run build` 时没有生成 Prisma Client，枚举与查询类型失效，首次构建出现 677 个 TypeScript 报错。
- 证据文件和行号：[package.json](/Users/nianbaidediannao/Desktop/后端后台本地测试版/backend/package.json:12)
- 根因判断：构建脚本依赖人工提前执行 `prisma generate`，但没有把该前置步骤纳入自身。
- 用户/运营影响：干净依赖目录、CI 或新部署节点可能无法构建，阻断发布。
- 最小修复建议：在构建脚本首步执行既有的 `db:generate`。
- 修复记录：已将 `npm run db:generate` 置于 `build` 首步；不新增依赖和额外构建流程。
- 验证方式：在同一命令内重新生成 Prisma Client 后，`npm run build` 通过。
- 当前状态：已验证。

## 本轮结论

- 已确认修复：AUD-P1-056、AUD-P1-057。
- 新发现并修复：NEW-P1-20260716-001。
- 仍待运行验证：真实微信 JSAPI 下单、回调、主动查单和预约释放任务的端到端联调。
- 环境风险：Git 对象包损坏，`git diff` 无法作为变更证据；原审计报告内容缺失，需先恢复文件内容再合并本记录。

## NEW-P2-20260716-002

- 严重级别：P2
- 所属模块：内容模块测试基座
- 问题现象：`PostService` 已注入 `GrowthService`，但单元测试未提供 mock，导致该测试套件 41 条断言在装配阶段全部失败。
- 证据文件和行号：[post.service.ts](/Users/nianbaidediannao/Desktop/后端后台本地测试版/backend/src/modules/post/post.service.ts:25)、[post.service.spec.ts](/Users/nianbaidediannao/Desktop/后端后台本地测试版/backend/src/modules/post/post.service.spec.ts:116)
- 根因判断：新增生产依赖没有同步到 Nest 测试模块的 provider 列表。
- 用户/运营影响：不直接改变线上逻辑，但阻断内容发布、点赞、匿名身份等回归测试，可能掩盖市场版内容链路回归。
- 最小修复建议：为 `GrowthService` 提供仅含 `getContentBoostByUserIds` 的 mock。
- 验证方式：`src/modules/post/post.service.spec.ts` 41 条通过；全量 `npm test -- --runInBand` 57 套件、370 条通过；`npm run build` 通过。
- 当前状态：已验证。

## AUD-P0-004

- 严重级别：P0
- 所属模块：钱包提现、冻结余额与资金流水
- 问题现象：小程序提现页提交 `withdraw_type`、`receiver_name` 等兼容字段，而资金服务只读取 `channel`、`account`；后台审批链路也缺少当前页面可用的打款完成步骤。
- 证据文件和行号：[withdraw.js](/Users/nianbaidediannao/Desktop/前端文件/pagesA/withdraw/withdraw.js:1)、[finance.service.ts](/Users/nianbaidediannao/Desktop/后端后台本地测试版/backend/src/modules/finance/finance.service.ts:79)、[WithdrawalsPage.vue](/Users/nianbaidediannao/Desktop/后端后台本地测试版/admin/src/views/finance/WithdrawalsPage.vue:34)
- 根因判断：小程序兼容接口直接透传原始 DTO，且新后台提现页面只实现了 `PENDING -> PROCESSING`，未串接 `PROCESSING -> SUCCESS` 和打款凭证。
- 用户/运营影响：用户提现可能因字段不匹配无法提交；已审核申请会停留处理中，冻结金额和财务流水无法形成可追踪闭环。
- 修改文件：`backend/src/modules/finance/finance.service.ts`、`backend/src/modules/finance-admin/finance-admin.service.ts`、`backend/src/modules/finance-admin/finance-admin.controller.ts`、`admin/src/views/finance/WithdrawalsPage.vue`、`backend/src/modules/admin/admin.service.ts`。
- 修复思路：服务端统一转换微信/支付宝兼容字段并校验金额、渠道和账号；申请时以余额条件更新冻结资金；审核、拒绝、完成打款均以原状态条件更新为门槛，并记录真实钱包余额、打款流水号和操作日志。
- 覆盖业务链路：小程序提现申请 -> `FinanceService.withdraw` -> `Wallet/Withdraw/WalletTransaction` -> 后台审核 -> 拒绝退回或确认打款 -> 冻结额释放、总支出和操作审计。
- 验证方式：新增 `finance.service.spec.ts`、`finance-admin.service.spec.ts`；提现相关 35 条定向测试通过，`npm run build` 通过。此前全量测试为 59 个套件、372 条通过；本轮最终微调后已重跑资金定向测试和构建。
- 当前状态：已修。仍需真实测试账户验证余额冻结、拒绝退回、带打款流水号完成提现和后台权限分离后，才能标记“已验证”。

## 本轮结论

- 已确认修复：AUD-P0-004 的小程序字段兼容、余额冻结、审核/拒绝/完成打款状态机和流水闭环。
- 仍待运行验证：真实微信/支付宝或人工打款渠道、余额账户初始数据、`finance:withdraw` 与 `withdraw:complete` 角色分离。
- 下一批：退款的回调终态与余额/订单/平台流水三方一致性。

## 记录状态更新

- `市场版总审计报告.md` 已恢复可读；本轮资金链修复记录已直接追加到主报告末尾，后续以主报告为准。
